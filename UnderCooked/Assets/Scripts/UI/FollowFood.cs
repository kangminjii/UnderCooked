using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class FollowFood : MonoBehaviour
{
    //private Transform target;
    //public Image uiImage;

    //void Start()
    //{
    //    target = this.gameObject.transform;//.transform.root;
    //    uiImage = GameObject.Find("PrawnImage").GetComponent<Image>();// 이거 해야된다
    //}

    //void Update()
    //{
    //    if (target != null && uiImage != null)
    //    {
    //        // 따라다니는 코드
    //        Vector3 screenPos = Camera.main.WorldToScreenPoint(target.position);
    //        uiImage.transform.position = screenPos;
    //    }
    //}
}
